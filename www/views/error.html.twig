{% extends "layout/base.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col-8 offset-2 alert alert-danger mt-4" role="alert">
            <h4 class="alert-heading">Error {{ description }}</h4>
            <hr>
            <a href="{{ uri("dashboard") }}"><button class="btn btn-primary btn-sm">{{ lang('general.error.returnDashboard') }}</button></a>
        </div>
    </div>
{% endblock %}
