<form action="" method="post" class="col-md-8 px-md-0">
    <div class="form-group">
        <input type="text" id="add_banned-players" name="add_banned-players" class="form-control" required="required" placeholder="Ajouter un joueur à la list">
    </div>
    <div class="form-group">
        <input type="text" id="reason" name="reason" class="form-control" placeholder="Raison">
    </div>
    <input type="hidden" name="token" value="{{token}}">
    <div class="form-group">
        <button class="btn btn-success btn-sm" type="submit">Ajouter</button>
    </div>
</form>

<table class="table table-striped table-sm table-bordered">
    <thead>
        <tr>
            <th>Username</th>
            <th>UUID</th>
            <th>Date</th>
            <th>Supprimer</th>
        </tr>
    </thead>

    <tbody>
    {% for player in bannedPlayers %}
        <tr>
            <td>{{player.name}}</td>
            <td>{{player.uuid}}</td>
            <td>{{player.created}}</td>
            <td><button class="btn btn-danger btn-sm float-right delete"><i class="far fa-trash-alt"></i></button></td>
        </tr>
    {% endfor %}
    </tbody>
</table>