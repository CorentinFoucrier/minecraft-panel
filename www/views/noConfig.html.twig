{% extends "layout/base.html.twig" %}

{% block content %}
    {% for errors in flash("alert") %}
        <div class="row">
            <div class="col-8 offset-2 alert alert-danger mt-4" role="alert">
                <h4 class="alert-heading">Oops! There is an error.</h4>
                <hr>
                <p>The "server.property" file not found!</p>
            </div>
        </div>
    {% endfor %}
{% endblock %}